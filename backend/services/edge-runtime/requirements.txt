# Edge runtime — enough to run the full closed loop locally with the mock
# provider, zero cloud creds. Vertex / local-VLM extras are added in P3 / P5.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
SQLAlchemy==2.0.36
python-multipart==0.0.20
httpx==0.28.1
# Image ingestion — iPhone (HEIC/HEIF), Samsung/Android, webcam/CSI camera frames.
Pillow==11.1.0
pillow-heif==0.21.0
